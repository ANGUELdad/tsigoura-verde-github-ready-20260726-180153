{
  "name": "tsigoura-verde-menu",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "@vercel/blob": "^2.6.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "scripts": {
    "test": "node tests/security-and-persistence.test.js && node tests/upload-security.test.js",
    "test:ui": "playwright test",
    "test:ui:update": "playwright test --update-snapshots"
  }
}
